# VXLAN Basic Configuration
# Description: Basic setup and configuration of a VXLAN tunnel.
# Topology: Two hosts connected via a VXLAN tunnel.
# Actions: Configure VXLAN tunnel between two hosts and test connectivity.

